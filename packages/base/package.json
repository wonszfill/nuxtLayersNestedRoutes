{
  "name": "base",
  "version": "0.0.1",
  "private": true,
  "main": "./nuxt.config.ts",
  "volta": {
    "extends": "../../package.json"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^18",
    "nuxt": "^3.5.1"
  }
}
